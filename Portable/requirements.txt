# Runtime dependencies for the RSW (Robust Spread-spectrum Watermark) system
numpy>=2.0
scipy>=1.11
pillow>=10.0
reedsolo>=1.7
brotli>=1.1
customtkinter>=5.2

# Build-only: install to produce the standalone executable, then run
#   pyinstaller RSW.spec
# (artifacts appear in dist/; see RSW.spec for per-OS details).
# pyinstaller>=6.0
