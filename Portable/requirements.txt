# Runtime dependencies for the ANS-STC robust steganography system
numpy>=2.0
scipy>=1.11
pillow>=10.0
reedsolo>=1.7
brotli>=1.1
customtkinter>=5.2

# Build-only: install to produce the standalone executable, then run
#   pyinstaller ANS-STC.spec
# (artifacts appear in dist/; see ANS-STC.spec for per-OS details).
# pyinstaller>=6.0
